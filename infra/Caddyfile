{
  auto_https off
}

# Optional: map port 80 → Vite (3000). Use the app at http://localhost:3000 or http://localhost when this container is up.
# API: http://localhost:1337/api/v1 (no proxy).
http://localhost, http://127.0.0.1 {
  reverse_proxy host.docker.internal:3000
}
