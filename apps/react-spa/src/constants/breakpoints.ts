/**
 * Default Tailwind `theme.screens` widths (min-width, px).
 * Update if `tailwind.config.ts` defines custom `screens`.
 */
export const BREAKPOINT_PX = {
  sm: 640,
  md: 768,
  lg: 1024,
  xl: 1280,
  "2xl": 1536,
} as const;
